{
  "name": "saas-project-mgmt",
  "private": true,
  "workspaces": [
    "apps/server",
    "apps/web"
  ],
  "scripts": {
    "dev": "concurrently -k \"npm:dev --workspace apps/server\" \"npm:dev --workspace apps/web\"",
    "build": "npm run build --workspace apps/server && npm run build --workspace apps/web"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
